# GuardDiff Docs Site

This is the dependency-free static documentation site for the v0.1.0 launch phase.

Open `index.html` locally to review the docs. Publish the whole directory as the site root so `/rules/manifest.json` is available for GitHub Action rule-update notifications.
