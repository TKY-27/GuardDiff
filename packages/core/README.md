# @guarddiff/core

Core scanner, built-in rules, parser, policy evaluator, suppression filter, and reporters for GuardDiff.

This package is published for integrations. Most users should install the CLI:

```bash
npm install -g guarddiff
```
